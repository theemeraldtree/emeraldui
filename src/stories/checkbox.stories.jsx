import React from 'react';
import { Checkbox } from '../main';

export default { title: 'Checkbox' };

export const simpleCheckbox = () => <Checkbox />;
export const lighterCheckbox = () => <Checkbox lighter />;
